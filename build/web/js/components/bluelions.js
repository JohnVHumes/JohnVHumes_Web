function bluelions(id) {

    // ` this is a "back tick". Use it to define multi-line strings in JavaScript.
    var content = `  
      
        Data listed from my character table - COMING SOON !
      
    `;
    document.getElementById(id).innerHTML = content;
}